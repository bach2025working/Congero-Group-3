Oddisey 3.5
Desc: $15 monthly with 100 included prompts AND $0.60 USD per extra prompt

Tested By Kevin:
  Email: kevin6@nai3.test
  Billed: $47.25
  Prompt input: 150
  Billed Prompt: 50
  Calculation: $15 (monthly) + $0.60 * 50 prompts (# of prompts billed) + 5% tax

Conclusion: Passed

#################################################################################

Seeding Prompt Code:
```
  (
    for i in $(seq 1 150)
      do
        perl -pi -e "s/PIN_FLD_TRANSACTION_ID       DECIMAL \[0\] [0-9.]+/PIN_FLD_TRANSACTION_ID       DECIMAL [0] 980$i.00/" ./nextaig3/load_usage.nap

        echo "< ./nextaig3/load_usage.nap"

        sleep 1
      done
    echo "q"
  ) | testnap
```

Checking Balance (in testnap):
```
r << END 1
0 PIN_FLD_POID POID [0] 0.0.0.1 /balance_group 970550 1
END

robj 1
```
  
